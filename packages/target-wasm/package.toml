schema = "ailang.package-source.v1"
name = "target-wasm"
version = "0.0.1-alpha.3"
types = ["target", "tool", "template"]

[dependencies]
std-http = "0.0.1-alpha.9"

[targets.wasm]
id = "wasm"
description = "WebAssembly and browser application target"
runtime = "aivm"
profile = "wasm-browser"
aliases = ["wasm32", "web"]
defaultRunner = "run"
artifactTypes = ["dir", "spa", "module"]
options = ["wasm-profile", "server", "open-browser", "port"]

[targets.wasm.tools]
run = []
publish = []
doctor = []
flash = []
